# figure_recognition

Real-time skeleton + per-keypoint confidence inference using RTMPose.

## Layout

```
figure_recognition/
├── README.md     this file
└── models/       downloaded model weights (gitignored)
```

## models/

Holds the downloaded `.pth` checkpoints — gitignored due to GitHub's 100 MB single-file limit. See the top-level [README](../README.md) for the `curl` commands that populate this directory.
